# Patch: issuer verification gaps

## Summary

Fixed issuer verification gaps found after plan `04-token-issuer-and-jwks` implementation.

## Changes

- Accepted real `SiteIdFactory` site IDs in `IssueUserApiTokenRequest`.
- Normalized issuer FormRequest validation errors to `error/message/request_id/fields`.
- Added regression coverage for real site IDs and deterministic validation errors.
- Updated issuer/JWKS docs to reflect the implemented token issue/revoke + JWKS runtime slice.
- Restored `.ai-factory/DESCRIPTION.md` encoding and removed trailing whitespace in touched files.

## Verification

- `php artisan test --without-tty --filter=IssuerApiFlowTest`
- `composer validate --strict`
- `php vendor\bin\pint --test`
- `php artisan test --without-tty`
- `git diff --check`
- targeted trailing-whitespace scan
- issuer/JWKS contradiction search
- route-list sanity checks for `api/v1/user` and `oauth`

All verification passed. `git diff --check` reported only CRLF normalization warnings for markdown files.

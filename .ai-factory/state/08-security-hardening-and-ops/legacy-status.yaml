status: planned
plan_id: 08-security-hardening-and-ops
area: "security, ops, laravel, issuer, gateway"
dependencies: "01-07 plans"
outputs: ["rate limits", "structured logs with request_id", "key rotation commands", "backup/restore notes", "security runbook", "CI security checks", "gateway stale key policy"]
acceptance: ["raw secrets не появляются в logs", "rate limits покрывают auth/token endpoints", "key rotation имеет documented flow", "есть runbook для leaked key/token", "CI выполняет tests/build/smoke"]

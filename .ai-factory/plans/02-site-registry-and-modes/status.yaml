id: 02-site-registry-and-modes
title: "Connected Site Registry & Modes"
status: planned
area: "site_registry, portal"
depends_on: ['01-platform-foundation']
outputs: ['sites schema', 'site_verifications schema', 'site_modes schema', 'POST /v1/sites', 'POST /v1/sites/{id}/verify', 'API resource settings', 'OIDC client settings skeleton']
acceptance: ['verified domain получает site_id', 'режим api_resource нельзя включить без verified domain', 'режим web_client требует exact redirect_uri']
notes: []

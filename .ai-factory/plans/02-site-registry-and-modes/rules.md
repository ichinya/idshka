# Local rules: 02-site-registry-and-modes

## Areas
site_registry, portal

## Additional constraints
- Follow global `.ai-factory/RULES.md`.
- Follow related area files from `.ai-factory/rules/`.
- Do not store raw secrets in fixtures unless clearly marked as fake demo secrets.
- Keep API examples aligned with `docs/API_FLOWS.md`.

status: planned
plan_id: 04-token-issuer-and-jwks
area: "issuer, security, laravel"
dependencies: "03-site-registry-and-modes"
outputs: ["signing_keys schema", "api_tokens schema", "revoked_jti schema", "GET /oauth/jwks.json", "POST /v1/user/api-tokens", "POST /v1/user/api-tokens/{id}/revoke", "TokenIssuer/JwksService/SigningKeyService"]
acceptance: ["можно выпустить токен для aud=apishka.ru", "JWKS содержит active public key по kid", "JWT содержит iss/aud/sub/site_id/scope/permissions/jti/exp", "raw token показывается один раз", "revoked jti появляется в БД/Redis"]
